# Frontend Development Environment Variables
# These are used when running "npm run dev"

# API Configuration - Route through APISIX Gateway
# This must point at the APISIX API domain (not the backend container/port directly)
VITE_API_BASE_URL=http://api.paapeli.local

# Authentication Provider
# Options: basic (for development), cognito (for production)
VITE_AUTH_PROVIDER=basic

# Application Settings
VITE_APP_NAME=Paapeli IoT Platform
VITE_APP_ENV=development
